export const metadata = {
  title: "My Tutors - MediQueue",
  description: "View and manage your list of tutors on MediQueue.",
};

export default function MyTutorsLayout({ children }) {
  return (
    <div>
      {children}
    </div>
  );
}