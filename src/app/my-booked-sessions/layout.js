export const metadata = {
  title: "My Sessions - MediQueue",
  description: "View and manage your booked tutoring sessions on MediQueue.",
};

export default function MyBookedSessionsLayout({ children }) {
  return (
    <div>
      {children}
    </div>
  );
}