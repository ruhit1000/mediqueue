export const metadata = {
  title: "About Us - MediQueue",
  description: "Learn more about MediQueue and our mission to connect students with expert tutors for personalized learning experiences.",
};

export default function AboutUsLayout({ children }) {
  return (
    <div>
      {children}
    </div>
  );
}