export const metadata = {
  title: "Add Tutor - MediQueue",
  description: "Add a new tutor to your MediQueue account.",
};

export default function AddTutorLayout({ children }) {
  return (
    <div>
      {children}
    </div>
  );
}