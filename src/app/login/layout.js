export const metadata = {
  title: "Login - MediQueue",
  description: "Access your MediQueue account and manage your tutoring sessions.",
};

export default function LoginLayout({ children }) {
  return (
    <div>
      {children}
    </div>
  );
}