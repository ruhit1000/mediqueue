export const metadata = {
  title: "Update Tutor - MediQueue",
  description: "Update your tutor information and manage your profile.",
};

export default function UpdateTutorLayout({ children }) {
  return (
    <div>
      {children}
    </div>
  );
}