export const metadata = {
  title: "My Profile - MediQueue",
  description: "View and manage your profile information on MediQueue.",
};

export default function MyProfileLayout({ children }) {
  return (
    <div>
      {children}
    </div>
  );
}