export const metadata = {
  title: "Register - MediQueue",
  description: "Create a new MediQueue account and start managing your tutoring sessions.",
};

export default function RegisterLayout({ children }) {
  return (
    <div>
      {children}
    </div>
  );
}