export const metadata = {
  title: "Tutors - MediQueue",
  description: "Browse our directory of qualified tutors and find the perfect match for your learning needs.",
};

export default function TutorsLayout({ children }) {
  return (
    <div>
      {children}
    </div>
  );
}